# Calculator app — copy to `.env.local` and fill in values.
#   cp .env.example .env.local

# Required for AI chat (`app/api/chat`). Get a key from Anthropic.
ANTHROPIC_API_KEY=

# Optional: max AI requests per hour for this server process (omit or empty = unlimited).
# MAX_CALCS_PER_HOUR=100
